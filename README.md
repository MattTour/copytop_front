# Front SupCup

# Start the project

Make sure the backend is running.

Start container with :

`docker-compose up`

Go to http://localhost:3000/

# Stop the project

 Stop container with :

 `docker-compose down`
